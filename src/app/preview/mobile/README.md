# Mobile layout preview (throwaway)

Self-contained preview at `/preview/mobile`. Not wired to responsive breakpoints.

**Delete this entire folder** to remove the preview:

```
src/app/preview/mobile/
```

No other files need to change.
